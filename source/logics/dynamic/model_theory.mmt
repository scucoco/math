namespace http://cds.omdoc.org/logic/model_theory/kripke

import syn ..
import minimal http://latin.omdoc.org/foundations/minimal

theory PDLFrame : ur:?LF =
  include minimal:?STTIFOLEQExt
  include minimal:?Universes
  
  K : set
  




view MeaningFun : http://cds.omdoc.org/logic?PDLMinimalSyntax -> ?PDLFrame =
  PDLProg = elem K → elem K → bool
  prop = elem K → bool  
	true = [w] 1 
  impl = [kφ][kψ] [w] (kφ w) ⇒ (kψ w) 
  necessity = [kα] [kφ] [u]  ∀([v] (kα u v) ⇒ (kφ v)) 
  compos = [kα] [kβ] [u] [v] [w] (kα u w) ∧ (kβ w v) 

  

  

